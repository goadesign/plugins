// Code generated by goa, DO NOT EDIT.
//
// test-jsonrpc JSON-RPC client CLI support package
//
// Command:
// $ goa gen goa.design/plugins/v3/testing/examples/jsonrpc/design -o
// $(GOPATH)/src/goa.design/plugins/testing//examples/jsonrpc

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
	testjsonrpcc "goa.design/plugins/v3/testing/examples/jsonrpc/gen/jsonrpc/test_jsonrpc/client"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"test-jsonrpc (jsonrpc-no-stream|jsonrpc-no-stream-error)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "test-jsonrpc jsonrpc-no-stream --body '{\n      \"msg\": \"Excepturi est sit quo quam.\"\n   }'" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		testJsonrpcFlags = flag.NewFlagSet("test-jsonrpc", flag.ContinueOnError)

		testJsonrpcJsonrpcNoStreamFlags    = flag.NewFlagSet("jsonrpc-no-stream", flag.ExitOnError)
		testJsonrpcJsonrpcNoStreamBodyFlag = testJsonrpcJsonrpcNoStreamFlags.String("body", "REQUIRED", "")

		testJsonrpcJsonrpcNoStreamErrorFlags    = flag.NewFlagSet("jsonrpc-no-stream-error", flag.ExitOnError)
		testJsonrpcJsonrpcNoStreamErrorBodyFlag = testJsonrpcJsonrpcNoStreamErrorFlags.String("body", "REQUIRED", "")
	)
	testJsonrpcFlags.Usage = testJsonrpcUsage
	testJsonrpcJsonrpcNoStreamFlags.Usage = testJsonrpcJsonrpcNoStreamUsage
	testJsonrpcJsonrpcNoStreamErrorFlags.Usage = testJsonrpcJsonrpcNoStreamErrorUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "test-jsonrpc":
			svcf = testJsonrpcFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "test-jsonrpc":
			switch epn {
			case "jsonrpc-no-stream":
				epf = testJsonrpcJsonrpcNoStreamFlags

			case "jsonrpc-no-stream-error":
				epf = testJsonrpcJsonrpcNoStreamErrorFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "test-jsonrpc":
			c := testjsonrpcc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "jsonrpc-no-stream":
				endpoint = c.JsonrpcNoStream()
				data, err = testjsonrpcc.BuildJsonrpcNoStreamPayload(*testJsonrpcJsonrpcNoStreamBodyFlag)
			case "jsonrpc-no-stream-error":
				endpoint = c.JsonrpcNoStreamError()
				data, err = testjsonrpcc.BuildJsonrpcNoStreamErrorPayload(*testJsonrpcJsonrpcNoStreamErrorBodyFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// testJsonrpcUsage displays the usage of the test-jsonrpc command and its
// subcommands.
func testJsonrpcUsage() {
	fmt.Fprintln(os.Stderr, `Testing plugin matrix across JSON-RPC transports (non-streaming)`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] test-jsonrpc COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    jsonrpc-no-stream: JsonrpcNoStream implements jsonrpc_no_stream.`)
	fmt.Fprintln(os.Stderr, `    jsonrpc-no-stream-error: JsonrpcNoStreamError implements jsonrpc_no_stream_error.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s test-jsonrpc COMMAND --help\n", os.Args[0])
}
func testJsonrpcJsonrpcNoStreamUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] test-jsonrpc jsonrpc-no-stream", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `JsonrpcNoStream implements jsonrpc_no_stream.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "test-jsonrpc jsonrpc-no-stream --body '{\n      \"msg\": \"Excepturi est sit quo quam.\"\n   }'")
}

func testJsonrpcJsonrpcNoStreamErrorUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] test-jsonrpc jsonrpc-no-stream-error", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `JsonrpcNoStreamError implements jsonrpc_no_stream_error.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "test-jsonrpc jsonrpc-no-stream-error --body '{\n      \"msg\": \"Fuga illum cum voluptatem.\"\n   }'")
}
