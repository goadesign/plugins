// Code generated by goa, DO NOT EDIT.
//
// test-http-grpc gRPC client CLI support package
//
// Command:
// $ goa gen goa.design/plugins/v3/testing/examples/httpgrpc/design -o
// $(GOPATH)/src/goa.design/plugins/testing//examples/httpgrpc

package cli

import (
	"flag"
	"fmt"
	"os"

	goa "goa.design/goa/v3/pkg"
	testhttpgrpcc "goa.design/plugins/v3/testing/examples/httpgrpc/gen/grpc/test_http_grpc/client"
	grpc "google.golang.org/grpc"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"test-http-grpc (grpc-no-stream|grpc-no-stream-error-div-by-zero|grpc-server-stream|grpc-client-stream|grpc-bidi-stream|mixed-no-stream|mixed-server-stream|mixed-client-stream-ws-grpc|mixed-bidi-stream-ws-grpc)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "test-http-grpc grpc-no-stream --message '{\n      \"msg\": \"Ratione earum et ut temporibus tempora praesentium.\"\n   }'" + "\n" +
		""
}

// cliStringFlag keeps an omitted command-line flag distinct from an explicitly empty flag.
type cliStringFlag struct {
	value *string
}

// String returns the flag text shown by the standard flag package.
func (f *cliStringFlag) String() string {
	if f.value == nil {
		return ""
	}
	return *f.value
}

// Set records that the user supplied the flag, even when value is empty.
func (f *cliStringFlag) Set(value string) error {
	f.value = &value
	return nil
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	cc *grpc.ClientConn,
	opts ...grpc.CallOption,
) (goa.Endpoint, any, error) {
	var (
		testHTTPGrpcFlags = flag.NewFlagSet("test-http-grpc", flag.ContinueOnError)

		testHTTPGrpcGrpcNoStreamFlags       = flag.NewFlagSet("grpc-no-stream", flag.ExitOnError)
		testHTTPGrpcGrpcNoStreamMessageFlag = new(cliStringFlag)

		testHTTPGrpcGrpcNoStreamErrorDivByZeroFlags       = flag.NewFlagSet("grpc-no-stream-error-div-by-zero", flag.ExitOnError)
		testHTTPGrpcGrpcNoStreamErrorDivByZeroMessageFlag = new(cliStringFlag)

		testHTTPGrpcGrpcServerStreamFlags       = flag.NewFlagSet("grpc-server-stream", flag.ExitOnError)
		testHTTPGrpcGrpcServerStreamMessageFlag = new(cliStringFlag)

		testHTTPGrpcGrpcClientStreamFlags = flag.NewFlagSet("grpc-client-stream", flag.ExitOnError)

		testHTTPGrpcGrpcBidiStreamFlags = flag.NewFlagSet("grpc-bidi-stream", flag.ExitOnError)

		testHTTPGrpcMixedNoStreamFlags       = flag.NewFlagSet("mixed-no-stream", flag.ExitOnError)
		testHTTPGrpcMixedNoStreamMessageFlag = new(cliStringFlag)

		testHTTPGrpcMixedServerStreamFlags = flag.NewFlagSet("mixed-server-stream", flag.ExitOnError)

		testHTTPGrpcMixedClientStreamWsGrpcFlags = flag.NewFlagSet("mixed-client-stream-ws-grpc", flag.ExitOnError)

		testHTTPGrpcMixedBidiStreamWsGrpcFlags = flag.NewFlagSet("mixed-bidi-stream-ws-grpc", flag.ExitOnError)
	)
	testHTTPGrpcGrpcNoStreamFlags.Var(testHTTPGrpcGrpcNoStreamMessageFlag, "message", "")
	testHTTPGrpcGrpcNoStreamErrorDivByZeroFlags.Var(testHTTPGrpcGrpcNoStreamErrorDivByZeroMessageFlag, "message", "")
	testHTTPGrpcGrpcServerStreamFlags.Var(testHTTPGrpcGrpcServerStreamMessageFlag, "message", "")
	testHTTPGrpcMixedNoStreamFlags.Var(testHTTPGrpcMixedNoStreamMessageFlag, "message", "")

	testHTTPGrpcFlags.Usage = testHTTPGrpcUsage
	testHTTPGrpcGrpcNoStreamFlags.Usage = testHTTPGrpcGrpcNoStreamUsage
	testHTTPGrpcGrpcNoStreamErrorDivByZeroFlags.Usage = testHTTPGrpcGrpcNoStreamErrorDivByZeroUsage
	testHTTPGrpcGrpcServerStreamFlags.Usage = testHTTPGrpcGrpcServerStreamUsage
	testHTTPGrpcGrpcClientStreamFlags.Usage = testHTTPGrpcGrpcClientStreamUsage
	testHTTPGrpcGrpcBidiStreamFlags.Usage = testHTTPGrpcGrpcBidiStreamUsage
	testHTTPGrpcMixedNoStreamFlags.Usage = testHTTPGrpcMixedNoStreamUsage
	testHTTPGrpcMixedServerStreamFlags.Usage = testHTTPGrpcMixedServerStreamUsage
	testHTTPGrpcMixedClientStreamWsGrpcFlags.Usage = testHTTPGrpcMixedClientStreamWsGrpcUsage
	testHTTPGrpcMixedBidiStreamWsGrpcFlags.Usage = testHTTPGrpcMixedBidiStreamWsGrpcUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "test-http-grpc":
			svcf = testHTTPGrpcFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "test-http-grpc":
			switch epn {
			case "grpc-no-stream":
				epf = testHTTPGrpcGrpcNoStreamFlags

			case "grpc-no-stream-error-div-by-zero":
				epf = testHTTPGrpcGrpcNoStreamErrorDivByZeroFlags

			case "grpc-server-stream":
				epf = testHTTPGrpcGrpcServerStreamFlags

			case "grpc-client-stream":
				epf = testHTTPGrpcGrpcClientStreamFlags

			case "grpc-bidi-stream":
				epf = testHTTPGrpcGrpcBidiStreamFlags

			case "mixed-no-stream":
				epf = testHTTPGrpcMixedNoStreamFlags

			case "mixed-server-stream":
				epf = testHTTPGrpcMixedServerStreamFlags

			case "mixed-client-stream-ws-grpc":
				epf = testHTTPGrpcMixedClientStreamWsGrpcFlags

			case "mixed-bidi-stream-ws-grpc":
				epf = testHTTPGrpcMixedBidiStreamWsGrpcFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "test-http-grpc":
			c := testhttpgrpcc.NewClient(cc, opts...)
			switch epn {
			case "grpc-no-stream":
				endpoint = c.GrpcNoStream()
				data, err = testhttpgrpcc.BuildGrpcNoStreamPayload(testHTTPGrpcGrpcNoStreamMessageFlag.value)
			case "grpc-no-stream-error-div-by-zero":
				endpoint = c.GrpcNoStreamErrorDivByZero()
				data, err = testhttpgrpcc.BuildGrpcNoStreamErrorDivByZeroPayload(testHTTPGrpcGrpcNoStreamErrorDivByZeroMessageFlag.value)
			case "grpc-server-stream":
				endpoint = c.GrpcServerStream()
				data, err = testhttpgrpcc.BuildGrpcServerStreamPayload(testHTTPGrpcGrpcServerStreamMessageFlag.value)
			case "grpc-client-stream":
				endpoint = c.GrpcClientStream()
			case "grpc-bidi-stream":
				endpoint = c.GrpcBidiStream()
			case "mixed-no-stream":
				endpoint = c.MixedNoStream()
				data, err = testhttpgrpcc.BuildMixedNoStreamPayload(testHTTPGrpcMixedNoStreamMessageFlag.value)
			case "mixed-server-stream":
				endpoint = c.MixedServerStream()
			case "mixed-client-stream-ws-grpc":
				endpoint = c.MixedClientStreamWsGrpc()
			case "mixed-bidi-stream-ws-grpc":
				endpoint = c.MixedBidiStreamWsGrpc()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// testHTTPGrpcUsage displays the usage of the test-http-grpc command and its
// subcommands.
func testHTTPGrpcUsage() {
	fmt.Fprintln(os.Stderr, `Testing plugin matrix across HTTP and gRPC transports`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] test-http-grpc COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    grpc-no-stream: GrpcNoStream implements grpc_no_stream.`)
	fmt.Fprintln(os.Stderr, `    grpc-no-stream-error-div-by-zero: GrpcNoStreamErrorDivByZero implements grpc_no_stream_error_div_by_zero.`)
	fmt.Fprintln(os.Stderr, `    grpc-server-stream: GrpcServerStream implements grpc_server_stream.`)
	fmt.Fprintln(os.Stderr, `    grpc-client-stream: GrpcClientStream implements grpc_client_stream.`)
	fmt.Fprintln(os.Stderr, `    grpc-bidi-stream: GrpcBidiStream implements grpc_bidi_stream.`)
	fmt.Fprintln(os.Stderr, `    mixed-no-stream: MixedNoStream implements mixed_no_stream.`)
	fmt.Fprintln(os.Stderr, `    mixed-server-stream: MixedServerStream implements mixed_server_stream.`)
	fmt.Fprintln(os.Stderr, `    mixed-client-stream-ws-grpc: MixedClientStreamWsGrpc implements mixed_client_stream_ws_grpc.`)
	fmt.Fprintln(os.Stderr, `    mixed-bidi-stream-ws-grpc: MixedBidiStreamWsGrpc implements mixed_bidi_stream_ws_grpc.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s test-http-grpc COMMAND --help\n", os.Args[0])
}
func testHTTPGrpcGrpcNoStreamUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] test-http-grpc grpc-no-stream", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `GrpcNoStream implements grpc_no_stream.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "test-http-grpc grpc-no-stream --message '{\n      \"msg\": \"Ratione earum et ut temporibus tempora praesentium.\"\n   }'")
}

func testHTTPGrpcGrpcNoStreamErrorDivByZeroUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] test-http-grpc grpc-no-stream-error-div-by-zero", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `GrpcNoStreamErrorDivByZero implements grpc_no_stream_error_div_by_zero.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "test-http-grpc grpc-no-stream-error-div-by-zero --message '{\n      \"dividend\": 238.4063147460167,\n      \"divisor\": -73.4004594816786\n   }'")
}

func testHTTPGrpcGrpcServerStreamUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] test-http-grpc grpc-server-stream", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `GrpcServerStream implements grpc_server_stream.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "test-http-grpc grpc-server-stream --message '{\n      \"from\": 436641374502740749\n   }'")
}

func testHTTPGrpcGrpcClientStreamUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] test-http-grpc grpc-client-stream", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `GrpcClientStream implements grpc_client_stream.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "test-http-grpc grpc-client-stream")
}

func testHTTPGrpcGrpcBidiStreamUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] test-http-grpc grpc-bidi-stream", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `GrpcBidiStream implements grpc_bidi_stream.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "test-http-grpc grpc-bidi-stream")
}

func testHTTPGrpcMixedNoStreamUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] test-http-grpc mixed-no-stream", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `MixedNoStream implements mixed_no_stream.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "test-http-grpc mixed-no-stream --message '{\n      \"msg\": \"Ut non id provident tempora suscipit quo.\"\n   }'")
}

func testHTTPGrpcMixedServerStreamUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] test-http-grpc mixed-server-stream", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `MixedServerStream implements mixed_server_stream.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "test-http-grpc mixed-server-stream")
}

func testHTTPGrpcMixedClientStreamWsGrpcUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] test-http-grpc mixed-client-stream-ws-grpc", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `MixedClientStreamWsGrpc implements mixed_client_stream_ws_grpc.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "test-http-grpc mixed-client-stream-ws-grpc")
}

func testHTTPGrpcMixedBidiStreamWsGrpcUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] test-http-grpc mixed-bidi-stream-ws-grpc", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `MixedBidiStreamWsGrpc implements mixed_bidi_stream_ws_grpc.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "test-http-grpc mixed-bidi-stream-ws-grpc")
}
