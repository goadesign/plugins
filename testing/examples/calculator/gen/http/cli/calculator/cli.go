// Code generated by goa v3.22.1, DO NOT EDIT.
//
// calculator HTTP client CLI support package
//
// Command:
// $ goa gen goa.design/plugins/v3/testing/examples/calculator/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
	calculatorc "goa.design/plugins/v3/testing/examples/calculator/gen/http/calculator/client"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"calculator (add|divide|factorial|statistics|batch-add)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` calculator add --body '{
      "a": 0.48385320048391656,
      "b": 0.9771980381809557
   }'` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
	dialer goahttp.Dialer,
	calculatorConfigurer *calculatorc.ConnConfigurer,
) (goa.Endpoint, any, error) {
	var (
		calculatorFlags = flag.NewFlagSet("calculator", flag.ContinueOnError)

		calculatorAddFlags    = flag.NewFlagSet("add", flag.ExitOnError)
		calculatorAddBodyFlag = calculatorAddFlags.String("body", "REQUIRED", "")

		calculatorDivideFlags    = flag.NewFlagSet("divide", flag.ExitOnError)
		calculatorDivideBodyFlag = calculatorDivideFlags.String("body", "REQUIRED", "")

		calculatorFactorialFlags    = flag.NewFlagSet("factorial", flag.ExitOnError)
		calculatorFactorialBodyFlag = calculatorFactorialFlags.String("body", "REQUIRED", "")

		calculatorStatisticsFlags    = flag.NewFlagSet("statistics", flag.ExitOnError)
		calculatorStatisticsBodyFlag = calculatorStatisticsFlags.String("body", "REQUIRED", "")

		calculatorBatchAddFlags = flag.NewFlagSet("batch-add", flag.ExitOnError)
	)
	calculatorFlags.Usage = calculatorUsage
	calculatorAddFlags.Usage = calculatorAddUsage
	calculatorDivideFlags.Usage = calculatorDivideUsage
	calculatorFactorialFlags.Usage = calculatorFactorialUsage
	calculatorStatisticsFlags.Usage = calculatorStatisticsUsage
	calculatorBatchAddFlags.Usage = calculatorBatchAddUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "calculator":
			svcf = calculatorFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "calculator":
			switch epn {
			case "add":
				epf = calculatorAddFlags

			case "divide":
				epf = calculatorDivideFlags

			case "factorial":
				epf = calculatorFactorialFlags

			case "statistics":
				epf = calculatorStatisticsFlags

			case "batch-add":
				epf = calculatorBatchAddFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "calculator":
			c := calculatorc.NewClient(scheme, host, doer, enc, dec, restore, dialer, calculatorConfigurer)
			switch epn {
			case "add":
				endpoint = c.Add()
				data, err = calculatorc.BuildAddPayload(*calculatorAddBodyFlag)
			case "divide":
				endpoint = c.Divide()
				data, err = calculatorc.BuildDividePayload(*calculatorDivideBodyFlag)
			case "factorial":
				endpoint = c.Factorial()
				data, err = calculatorc.BuildFactorialPayload(*calculatorFactorialBodyFlag)
			case "statistics":
				endpoint = c.Statistics()
				data, err = calculatorc.BuildStatisticsPayload(*calculatorStatisticsBodyFlag)
			case "batch-add":
				endpoint = c.BatchAdd()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// calculatorUsage displays the usage of the calculator command and its
// subcommands.
func calculatorUsage() {
	fmt.Fprintf(os.Stderr, `A simple calculator service to demonstrate testing plugin features
Usage:
    %[1]s [globalflags] calculator COMMAND [flags]

COMMAND:
    add: Add two numbers
    divide: Divide two numbers
    factorial: Calculate factorial of a number
    statistics: Calculate statistics for a list of numbers
    batch-add: Add multiple pairs of numbers using streaming

Additional help:
    %[1]s calculator COMMAND --help
`, os.Args[0])
}
func calculatorAddUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] calculator add -body JSON

Add two numbers
    -body JSON: 

Example:
    %[1]s calculator add --body '{
      "a": 0.48385320048391656,
      "b": 0.9771980381809557
   }'
`, os.Args[0])
}

func calculatorDivideUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] calculator divide -body JSON

Divide two numbers
    -body JSON: 

Example:
    %[1]s calculator divide --body '{
      "dividend": 0.3381157086007379,
      "divisor": 0.10085904223482155
   }'
`, os.Args[0])
}

func calculatorFactorialUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] calculator factorial -body JSON

Calculate factorial of a number
    -body JSON: 

Example:
    %[1]s calculator factorial --body '{
      "n": 13
   }'
`, os.Args[0])
}

func calculatorStatisticsUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] calculator statistics -body JSON

Calculate statistics for a list of numbers
    -body JSON: 

Example:
    %[1]s calculator statistics --body '{
      "numbers": [
         0.8678700718070149,
         0.1307058221039593
      ]
   }'
`, os.Args[0])
}

func calculatorBatchAddUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] calculator batch-add

Add multiple pairs of numbers using streaming

Example:
    %[1]s calculator batch-add
`, os.Args[0])
}
