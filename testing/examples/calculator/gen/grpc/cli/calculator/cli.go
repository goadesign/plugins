// Code generated by goa, DO NOT EDIT.
//
// calculator gRPC client CLI support package
//
// Command:
// $ goa gen goa.design/plugins/v3/testing/examples/calculator/design -o
// $(GOPATH)/src/goa.design/plugins/testing//examples/calculator

package cli

import (
	"flag"
	"fmt"
	"os"

	goa "goa.design/goa/v3/pkg"
	calculatorc "goa.design/plugins/v3/testing/examples/calculator/gen/grpc/calculator/client"
	grpc "google.golang.org/grpc"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"calculator (add|divide|factorial|statistics|batch-add)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "calculator add --message '{\n      \"a\": 0.5324087495102858,\n      \"b\": 0.3676887822158714\n   }'" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	cc *grpc.ClientConn,
	opts ...grpc.CallOption,
) (goa.Endpoint, any, error) {
	var (
		calculatorFlags = flag.NewFlagSet("calculator", flag.ContinueOnError)

		calculatorAddFlags       = flag.NewFlagSet("add", flag.ExitOnError)
		calculatorAddMessageFlag = calculatorAddFlags.String("message", "", "")

		calculatorDivideFlags       = flag.NewFlagSet("divide", flag.ExitOnError)
		calculatorDivideMessageFlag = calculatorDivideFlags.String("message", "", "")

		calculatorFactorialFlags       = flag.NewFlagSet("factorial", flag.ExitOnError)
		calculatorFactorialMessageFlag = calculatorFactorialFlags.String("message", "", "")

		calculatorStatisticsFlags       = flag.NewFlagSet("statistics", flag.ExitOnError)
		calculatorStatisticsMessageFlag = calculatorStatisticsFlags.String("message", "", "")

		calculatorBatchAddFlags = flag.NewFlagSet("batch-add", flag.ExitOnError)
	)
	calculatorFlags.Usage = calculatorUsage
	calculatorAddFlags.Usage = calculatorAddUsage
	calculatorDivideFlags.Usage = calculatorDivideUsage
	calculatorFactorialFlags.Usage = calculatorFactorialUsage
	calculatorStatisticsFlags.Usage = calculatorStatisticsUsage
	calculatorBatchAddFlags.Usage = calculatorBatchAddUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "calculator":
			svcf = calculatorFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "calculator":
			switch epn {
			case "add":
				epf = calculatorAddFlags

			case "divide":
				epf = calculatorDivideFlags

			case "factorial":
				epf = calculatorFactorialFlags

			case "statistics":
				epf = calculatorStatisticsFlags

			case "batch-add":
				epf = calculatorBatchAddFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "calculator":
			c := calculatorc.NewClient(cc, opts...)
			switch epn {
			case "add":
				endpoint = c.Add()
				data, err = calculatorc.BuildAddPayload(*calculatorAddMessageFlag)
			case "divide":
				endpoint = c.Divide()
				data, err = calculatorc.BuildDividePayload(*calculatorDivideMessageFlag)
			case "factorial":
				endpoint = c.Factorial()
				data, err = calculatorc.BuildFactorialPayload(*calculatorFactorialMessageFlag)
			case "statistics":
				endpoint = c.Statistics()
				data, err = calculatorc.BuildStatisticsPayload(*calculatorStatisticsMessageFlag)
			case "batch-add":
				endpoint = c.BatchAdd()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// calculatorUsage displays the usage of the calculator command and its
// subcommands.
func calculatorUsage() {
	fmt.Fprintln(os.Stderr, `A simple calculator service to demonstrate testing plugin features`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] calculator COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    add: Add two numbers`)
	fmt.Fprintln(os.Stderr, `    divide: Divide two numbers`)
	fmt.Fprintln(os.Stderr, `    factorial: Calculate factorial of a number`)
	fmt.Fprintln(os.Stderr, `    statistics: Calculate statistics for a list of numbers`)
	fmt.Fprintln(os.Stderr, `    batch-add: Add multiple pairs of numbers using streaming`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s calculator COMMAND --help\n", os.Args[0])
}
func calculatorAddUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] calculator add", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Add two numbers`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "calculator add --message '{\n      \"a\": 0.5324087495102858,\n      \"b\": 0.3676887822158714\n   }'")
}

func calculatorDivideUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] calculator divide", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Divide two numbers`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "calculator divide --message '{\n      \"dividend\": 0.09668276034025738,\n      \"divisor\": 0.895624710740812\n   }'")
}

func calculatorFactorialUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] calculator factorial", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Calculate factorial of a number`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "calculator factorial --message '{\n      \"n\": 5\n   }'")
}

func calculatorStatisticsUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] calculator statistics", os.Args[0])
	fmt.Fprint(os.Stderr, " -message JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Calculate statistics for a list of numbers`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -message JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "calculator statistics --message '{\n      \"numbers\": [\n         0.9092932676423547\n      ]\n   }'")
}

func calculatorBatchAddUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] calculator batch-add", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Add multiple pairs of numbers using streaming`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "calculator batch-add")
}
