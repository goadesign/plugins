// Code generated by goa, DO NOT EDIT.
//
// Test client for calculator service
//
// Command:
// $ goa gen goa.design/plugins/v3/testing/examples/calculator/design -o
// $(GOPATH)/src/goa.design/plugins/testing//examples/calculator

package calculatortest

import (
	"context"
	"fmt"
	"testing"
	"time"

	calculator "goa.design/plugins/v3/testing/examples/calculator/gen/calculator"
	grpccli "goa.design/plugins/v3/testing/examples/calculator/gen/grpc/calculator/client"
	httpcli "goa.design/plugins/v3/testing/examples/calculator/gen/http/calculator/client"
)

// Transport defines the transport to use for client calls.
type Transport int

const (
	// AutoTransport uses the first available transport (default).
	AutoTransport Transport = iota
	// HTTPTransport forces HTTP transport usage.
	HTTPTransport
	// GRPCTransport forces gRPC transport usage.
	GRPCTransport
)

// Client provides a fluent API for calling calculator service methods through
// various transports.
type Client struct {
	httpClient *httpcli.Client
	grpcClient *grpccli.Client

	// Configuration
	transport Transport
	streaming bool // For mixed results, select streaming variant
	timeout   time.Duration

	// Test support
	t *testing.T
}

// HTTP returns a new client configured to use HTTP transport.
func (c *Client) HTTP() *Client {
	nc := *c
	nc.transport = HTTPTransport
	return &nc
}

// GRPC returns a new client configured to use gRPC transport.
func (c *Client) GRPC() *Client {
	nc := *c
	nc.transport = GRPCTransport
	return &nc
}

// AsStream returns a new client configured to request streaming variants for
// mixed results.
// This is only relevant for methods with SSE endpoints that support content
// negotiation.
func (c *Client) AsStream() *Client {
	nc := *c
	nc.streaming = true
	return &nc
}

// WithTimeout returns a new client with the specified timeout.
func (c *Client) WithTimeout(timeout time.Duration) *Client {
	nc := *c
	nc.timeout = timeout
	return &nc
}

// Add calls the add method using the configured transport.
func (c *Client) Add(ctx context.Context, p *calculator.AddPayload) (*calculator.AddResult, error) {
	// Determine which transport to use
	transport := c.transport
	if transport == AutoTransport {
		// Use the first available transport
		if c.httpClient != nil {
			transport = HTTPTransport
		} else if c.grpcClient != nil {
			transport = GRPCTransport
		}
	}

	switch transport {
	case HTTPTransport:
		if c.httpClient == nil {
			return nil, fmt.Errorf("HTTP transport not configured")
		}
		endpoint := c.httpClient.Add()
		res, err := endpoint(ctx, p)
		if err != nil {
			return nil, err
		}
		return res.(*calculator.AddResult), nil
	case GRPCTransport:
		if c.grpcClient == nil {
			return nil, fmt.Errorf("gRPC transport not configured")
		}
		endpoint := c.grpcClient.Add()
		res, err := endpoint(ctx, p)
		if err != nil {
			return nil, err
		}
		return res.(*calculator.AddResult), nil

	default:
		return nil, fmt.Errorf("no transport available for add")
	}
}

// Divide calls the divide method using the configured transport.
func (c *Client) Divide(ctx context.Context, p *calculator.DividePayload) (*calculator.DivideResult, error) {
	// Determine which transport to use
	transport := c.transport
	if transport == AutoTransport {
		// Use the first available transport
		if c.httpClient != nil {
			transport = HTTPTransport
		} else if c.grpcClient != nil {
			transport = GRPCTransport
		}
	}

	switch transport {
	case HTTPTransport:
		if c.httpClient == nil {
			return nil, fmt.Errorf("HTTP transport not configured")
		}
		endpoint := c.httpClient.Divide()
		res, err := endpoint(ctx, p)
		if err != nil {
			return nil, err
		}
		return res.(*calculator.DivideResult), nil
	case GRPCTransport:
		if c.grpcClient == nil {
			return nil, fmt.Errorf("gRPC transport not configured")
		}
		endpoint := c.grpcClient.Divide()
		res, err := endpoint(ctx, p)
		if err != nil {
			return nil, err
		}
		return res.(*calculator.DivideResult), nil

	default:
		return nil, fmt.Errorf("no transport available for divide")
	}
}

// Factorial calls the factorial method using the configured transport.
func (c *Client) Factorial(ctx context.Context, p *calculator.FactorialPayload) (*calculator.FactorialResult, error) {
	// Determine which transport to use
	transport := c.transport
	if transport == AutoTransport {
		// Use the first available transport
		if c.httpClient != nil {
			transport = HTTPTransport
		} else if c.grpcClient != nil {
			transport = GRPCTransport
		}
	}

	switch transport {
	case HTTPTransport:
		if c.httpClient == nil {
			return nil, fmt.Errorf("HTTP transport not configured")
		}
		endpoint := c.httpClient.Factorial()
		res, err := endpoint(ctx, p)
		if err != nil {
			return nil, err
		}
		return res.(*calculator.FactorialResult), nil
	case GRPCTransport:
		if c.grpcClient == nil {
			return nil, fmt.Errorf("gRPC transport not configured")
		}
		endpoint := c.grpcClient.Factorial()
		res, err := endpoint(ctx, p)
		if err != nil {
			return nil, err
		}
		return res.(*calculator.FactorialResult), nil

	default:
		return nil, fmt.Errorf("no transport available for factorial")
	}
}

// Statistics calls the statistics method using the configured transport.
func (c *Client) Statistics(ctx context.Context, p *calculator.StatisticsPayload) (*calculator.StatisticsResult, error) {
	// Determine which transport to use
	transport := c.transport
	if transport == AutoTransport {
		// Use the first available transport
		if c.httpClient != nil {
			transport = HTTPTransport
		} else if c.grpcClient != nil {
			transport = GRPCTransport
		}
	}

	switch transport {
	case HTTPTransport:
		if c.httpClient == nil {
			return nil, fmt.Errorf("HTTP transport not configured")
		}
		endpoint := c.httpClient.Statistics()
		res, err := endpoint(ctx, p)
		if err != nil {
			return nil, err
		}
		return res.(*calculator.StatisticsResult), nil
	case GRPCTransport:
		if c.grpcClient == nil {
			return nil, fmt.Errorf("gRPC transport not configured")
		}
		endpoint := c.grpcClient.Statistics()
		res, err := endpoint(ctx, p)
		if err != nil {
			return nil, err
		}
		return res.(*calculator.StatisticsResult), nil

	default:
		return nil, fmt.Errorf("no transport available for statistics")
	}
}

// BatchAdd calls the batch_add method using the configured transport.
func (c *Client) BatchAdd(ctx context.Context) (calculator.BatchAddClientStream, error) {
	// Determine which transport to use
	transport := c.transport
	if transport == AutoTransport {
		// Use the first available transport
		if c.httpClient != nil {
			transport = HTTPTransport
		} else if c.grpcClient != nil {
			transport = GRPCTransport
		}
	}

	switch transport {
	case HTTPTransport:
		if c.httpClient == nil {
			return nil, fmt.Errorf("HTTP transport not configured")
		}
		endpoint := c.httpClient.BatchAdd()
		res, err := endpoint(ctx, nil)
		if err != nil {
			return nil, err
		}
		return res.(calculator.BatchAddClientStream), nil
	case GRPCTransport:
		if c.grpcClient == nil {
			return nil, fmt.Errorf("gRPC transport not configured")
		}
		endpoint := c.grpcClient.BatchAdd()
		res, err := endpoint(ctx, nil)
		if err != nil {
			return nil, err
		}
		return res.(calculator.BatchAddClientStream), nil

	default:
		return nil, fmt.Errorf("no transport available for batch_add")
	}
}
