// Code generated by goa, DO NOT EDIT.
//
// tscap_example HTTP client CLI support package
//
// Command:
// $ goa gen goa.design/plugins/v3/tscap/example/design -o
// $(GOPATH)/src/goa.design/plugins/tscap//example

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
	tscapc "goa.design/plugins/v3/tscap/example/gen/http/tscap/client"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"tscap (list|create|admin|health)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "tscap list" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		tscapFlags = flag.NewFlagSet("tscap", flag.ContinueOnError)

		tscapListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		tscapCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		tscapCreateBodyFlag = tscapCreateFlags.String("body", "REQUIRED", "")

		tscapAdminFlags  = flag.NewFlagSet("admin", flag.ExitOnError)
		tscapAdminIDFlag = tscapAdminFlags.String("id", "REQUIRED", "Item ID")

		tscapHealthFlags = flag.NewFlagSet("health", flag.ExitOnError)
	)
	tscapFlags.Usage = tscapUsage
	tscapListFlags.Usage = tscapListUsage
	tscapCreateFlags.Usage = tscapCreateUsage
	tscapAdminFlags.Usage = tscapAdminUsage
	tscapHealthFlags.Usage = tscapHealthUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "tscap":
			svcf = tscapFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "tscap":
			switch epn {
			case "list":
				epf = tscapListFlags

			case "create":
				epf = tscapCreateFlags

			case "admin":
				epf = tscapAdminFlags

			case "health":
				epf = tscapHealthFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "tscap":
			c := tscapc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
			case "create":
				endpoint = c.Create()
				data, err = tscapc.BuildCreatePayload(*tscapCreateBodyFlag)
			case "admin":
				endpoint = c.Admin()
				data, err = tscapc.BuildAdminPayload(*tscapAdminIDFlag)
			case "health":
				endpoint = c.Health()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// tscapUsage displays the usage of the tscap command and its subcommands.
func tscapUsage() {
	fmt.Fprintln(os.Stderr, `A service demonstrating Tailscale app capabilities`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] tscap COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List items - requires read capability`)
	fmt.Fprintln(os.Stderr, `    create: Create an item - requires write capability`)
	fmt.Fprintln(os.Stderr, `    admin: Admin action - requires admin capability`)
	fmt.Fprintln(os.Stderr, `    health: Health check - no capability required`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s tscap COMMAND --help\n", os.Args[0])
}
func tscapListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] tscap list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List items - requires read capability`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "tscap list")
}

func tscapCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] tscap create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Create an item - requires write capability`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "tscap create --body '{\n      \"name\": \"Quo omnis quia ipsum.\"\n   }'")
}

func tscapAdminUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] tscap admin", os.Args[0])
	fmt.Fprint(os.Stderr, " -id STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Admin action - requires admin capability`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id STRING: Item ID`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "tscap admin --id \"Quidem eos voluptas eveniet.\"")
}

func tscapHealthUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] tscap health", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Health check - no capability required`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "tscap health")
}
