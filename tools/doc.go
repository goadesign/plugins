// Package tools implements the Goa plugin that generates canonical tool
// specifications. It inspects the design DSL to produce a registry of tool
// payloads and results, complete with JSON codecs and JSON Schema blobs that
// downstream services can reuse for serialization, validation, and metadata.
package tools
