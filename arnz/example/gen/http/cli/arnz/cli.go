// Code generated by goa v3.25.2, DO NOT EDIT.
//
// Arnz HTTP client CLI support package
//
// Command:
// $ goa gen goa.design/plugins/v3/arnz/example/design -o
// $(GOPATH)/src/goa.design/plugins/arnz//example

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
	arnzc "goa.design/plugins/v3/arnz/example/gen/http/arnz/client"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"arnz (create|read|update|delete|health)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "arnz create" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		arnzFlags = flag.NewFlagSet("arnz", flag.ContinueOnError)

		arnzCreateFlags = flag.NewFlagSet("create", flag.ExitOnError)

		arnzReadFlags = flag.NewFlagSet("read", flag.ExitOnError)

		arnzUpdateFlags = flag.NewFlagSet("update", flag.ExitOnError)

		arnzDeleteFlags = flag.NewFlagSet("delete", flag.ExitOnError)

		arnzHealthFlags = flag.NewFlagSet("health", flag.ExitOnError)
	)
	arnzFlags.Usage = arnzUsage
	arnzCreateFlags.Usage = arnzCreateUsage
	arnzReadFlags.Usage = arnzReadUsage
	arnzUpdateFlags.Usage = arnzUpdateUsage
	arnzDeleteFlags.Usage = arnzDeleteUsage
	arnzHealthFlags.Usage = arnzHealthUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "arnz":
			svcf = arnzFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "arnz":
			switch epn {
			case "create":
				epf = arnzCreateFlags

			case "read":
				epf = arnzReadFlags

			case "update":
				epf = arnzUpdateFlags

			case "delete":
				epf = arnzDeleteFlags

			case "health":
				epf = arnzHealthFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "arnz":
			c := arnzc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "create":
				endpoint = c.Create()
			case "read":
				endpoint = c.Read()
			case "update":
				endpoint = c.Update()
			case "delete":
				endpoint = c.Delete()
			case "health":
				endpoint = c.Health()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// arnzUsage displays the usage of the arnz command and its subcommands.
func arnzUsage() {
	fmt.Fprintln(os.Stderr, `Service is the Arnz service interface.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] arnz COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    create: Create implements create.`)
	fmt.Fprintln(os.Stderr, `    read: Read implements read.`)
	fmt.Fprintln(os.Stderr, `    update: Update implements update.`)
	fmt.Fprintln(os.Stderr, `    delete: Delete implements delete.`)
	fmt.Fprintln(os.Stderr, `    health: Health implements health.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s arnz COMMAND --help\n", os.Args[0])
}
func arnzCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] arnz create", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Create implements create.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "arnz create")
}

func arnzReadUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] arnz read", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Read implements read.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "arnz read")
}

func arnzUpdateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] arnz update", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Update implements update.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "arnz update")
}

func arnzDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] arnz delete", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Delete implements delete.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "arnz delete")
}

func arnzHealthUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] arnz health", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Health implements health.`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "arnz health")
}
