// Code generated by goa v3.24.0, DO NOT EDIT.
//
// archiver HTTP client CLI support package
//
// Command:
// $ goa gen goa.design/plugins/v3/goakit/examples/fetcher/archiver/design -o
// $(GOPATH)/src/goa.design/plugins/goakit//examples/fetcher/archiver

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	"github.com/go-kit/kit/endpoint"
	goahttp "goa.design/goa/v3/http"
	archiverc "goa.design/plugins/v3/goakit/examples/fetcher/archiver/gen/http/archiver/client"
	healthc "goa.design/plugins/v3/goakit/examples/fetcher/archiver/gen/http/health/client"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"archiver (archive|read)",
		"health show",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "archiver archive --body '{\n      \"body\": \"Unde sed nulla.\",\n      \"status\": 200\n   }'" + "\n" +
		os.Args[0] + " " + "health show" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (endpoint.Endpoint, any, error) {
	var (
		archiverFlags = flag.NewFlagSet("archiver", flag.ContinueOnError)

		archiverArchiveFlags    = flag.NewFlagSet("archive", flag.ExitOnError)
		archiverArchiveBodyFlag = archiverArchiveFlags.String("body", "REQUIRED", "")

		archiverReadFlags  = flag.NewFlagSet("read", flag.ExitOnError)
		archiverReadIDFlag = archiverReadFlags.String("id", "REQUIRED", "ID of archive")

		healthFlags = flag.NewFlagSet("health", flag.ContinueOnError)

		healthShowFlags = flag.NewFlagSet("show", flag.ExitOnError)
	)
	archiverFlags.Usage = archiverUsage
	archiverArchiveFlags.Usage = archiverArchiveUsage
	archiverReadFlags.Usage = archiverReadUsage

	healthFlags.Usage = healthUsage
	healthShowFlags.Usage = healthShowUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "archiver":
			svcf = archiverFlags
		case "health":
			svcf = healthFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "archiver":
			switch epn {
			case "archive":
				epf = archiverArchiveFlags

			case "read":
				epf = archiverReadFlags

			}

		case "health":
			switch epn {
			case "show":
				epf = healthShowFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint endpoint.Endpoint
		err      error
	)
	{
		switch svcn {
		case "archiver":
			c := archiverc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "archive":
				endpoint = c.Archive()
				data, err = archiverc.BuildArchivePayload(*archiverArchiveBodyFlag)
			case "read":
				endpoint = c.Read()
				data, err = archiverc.BuildReadPayload(*archiverReadIDFlag)
			}
		case "health":
			c := healthc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "show":
				endpoint = c.Show()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// archiverUsage displays the usage of the archiver command and its subcommands.
func archiverUsage() {
	fmt.Fprintln(os.Stderr, `Service is the archiver service interface.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] archiver COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    archive: Archive HTTP response`)
	fmt.Fprintln(os.Stderr, `    read: Read HTTP response from archive`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s archiver COMMAND --help\n", os.Args[0])
}
func archiverArchiveUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] archiver archive", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Archive HTTP response`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "archiver archive --body '{\n      \"body\": \"Unde sed nulla.\",\n      \"status\": 200\n   }'")
}

func archiverReadUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] archiver read", os.Args[0])
	fmt.Fprint(os.Stderr, " -id INT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Read HTTP response from archive`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id INT: ID of archive`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "archiver read --id 7421807053867881696")
}

// healthUsage displays the usage of the health command and its subcommands.
func healthUsage() {
	fmt.Fprintln(os.Stderr, `Service is the health service interface.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] health COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    show: Health check endpoint`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s health COMMAND --help\n", os.Args[0])
}
func healthShowUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] health show", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Health check endpoint`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "health show")
}
